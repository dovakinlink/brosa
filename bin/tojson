#!/usr/bin/env node
const path = require('path');

const args = process.argv.slice(2);

if (args.length < 1) {
  console.error("Usage: tojson file.js");
  process.exit(-1);
}

const file = require(path.normalize(path.join(process.cwd(), args[0])));
console.log(JSON.stringify(file));
