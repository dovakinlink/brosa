#!/usr/bin/env node
const server = require('universal-webpack').server;
const settings = require('./universal-webpack-settings');
const configuration = require('./webpackfile.json');

server(configuration, settings);
